plugins { id 'com.android.application' }

android {
    namespace 'com.anbernic.consoleui'
    compileSdk 35

    defaultConfig {
        applicationId 'com.anbernic.consoleui'
        minSdk 26
        targetSdk 35
        versionCode 1
        versionName "0.1.0"
    }
}

dependencies {
    // Intentionally dependency-light: native Android UI for a small, fast launcher.
}
